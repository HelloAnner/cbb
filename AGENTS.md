# CBB 项目协作说明

## 1. 项目定位

CBB 是一个面向 AI 的、经过验证的功能能力知识库。

它从已经成功落地的项目中采集某一项做得好的功能，保留必要的示例代码，同时提炼代码背后的需求、边界、架构决策、不变量、失败经验、验证方法和组合约束。AI 在建设新项目时，应理解并迁移这些经验，根据目标项目的技术栈和业务上下文重新设计，而不是机械复制旧项目。

CBB 不是：

- 通用组件包或可直接拼装的业务代码仓库；
- 某个源项目的镜像、备份或完整代码副本；
- 未经验证的方案收集箱；
- 只描述“怎么写”、却不解释“为什么”的代码片段集合；
- 允许 AI 跳过目标项目分析的脚手架。

CBB 的目标是让新的 AI 能够回答：

1. 这个功能解决什么问题，适用和不适用的场景是什么？
2. 哪些设计已经被真实项目验证？
3. 哪些约束和不变量不能被破坏？
4. 哪些实现只是源项目的局部选择，可以按目标项目调整？
5. 过去踩过什么坑，为什么会失败？
6. 如何用测试或其他证据证明新的实现仍然正确？
7. 它与其他功能域组合时会发生什么依赖、冲突或边界重叠？
8. 哪些是绝对不能突破的红线，触碰后会造成什么后果？

## 2. 内容组织约定

### 2.1 一级目录就是一个子功能域

根目录下每个非隐藏一级业务目录代表一个可独立理解、采集、验证和复用的子功能域，例如 `rbac/`、`rag/` 或 `user/`。

- 一个目录只表达一个清晰的子功能域。
- 不因源项目、编程语言或团队划分功能域。
- 同一功能在不同项目中的优秀实现应归入同一个功能域，通过来源和适用条件区分，不应按项目复制多个目录。
- 功能过大时应先明确边界；只有子能力能够独立说明和验证时，才拆成新的一级目录。
- 功能域内部可以建立 `examples/`、`tests/`、`evidence/` 等材料目录；这些是材料分类，不是新的一级功能域。

### 2.2 每个子功能域必须有自己的 `AGENTS.md`

`<feature>/AGENTS.md` 是该功能域面向 AI 的权威入口。AI 进入子目录后，必须先读取它，再读取示例或其他材料。

子功能域的 `AGENTS.md` 必须做到：

- 自包含地说明该功能的目标、状态、边界和使用方法，不能只丢一组链接；
- 区分“已验证事实”“设计建议”“待验证假设”；
- 给出示例代码地图，解释每个示例证明什么；
- 说明哪些内容可以适配，哪些不变量必须保留；
- 单独列出坑点、真实踩坑记录和不可突破的红线；
- 记录来源和验证条件，使结论可以追溯；
- 说明与其他功能域组合时的依赖、冲突和责任边界；
- 在材料不足时明确写“待采集”或“待验证”，不得根据目录名自行补全最佳实践。

根目录出现新的一级功能目录时，创建目录的同一次变更中必须创建它的 `AGENTS.md`。没有 `AGENTS.md` 的目录视为不完整，不能被新项目采用。

### 2.3 功能域状态

每个子功能域只能处于以下状态之一，并在其 `AGENTS.md` 顶部明确记录：

- `待采集`：只有功能意图或候选来源，还没有足够材料。
- `采集中`：正在整理来源、设计和示例，内容尚不能作为已验证实践使用。
- `待验证`：材料基本完整，但验证证据或复核尚未完成。
- `已验证`：满足本文的完成门禁，可以被新项目采用。
- `已过期`：来源、依赖或结论已经失效，必须重新验证。
- `已弃用`：存在更合适的功能域或方案，只保留历史原因和迁移指引。

只有 `已验证` 的内容才能称为 CBB 最佳实践。其他状态可以用于研究，但 AI 必须显式提示不确定性。

## 3. 从成熟项目采集功能的固定流程

每次采集都按以下顺序执行。不得从“复制代码”开始，也不得因为源项目能运行就直接标记为已验证。

### 3.1 固定的是质量门禁，不是探索路线

采集允许灵活探索。下面十个步骤定义的是最终必须覆盖的证据和完成门禁，不要求 AI 机械地只走一次线性流程。

AI 可以根据现场事实：

- 从入口代码继续追踪到测试、调用方、事件、持久化、权限或故障处理；
- 从事故记录反向定位根因、修复代码和回归测试；
- 在代码、文档、提交历史和运行证据之间来回交叉验证；
- 发现原任务没有点名、但会影响正确性的相邻边界、隐含依赖和失败模式；
- 发现值得独立采集的新功能域，并先登记为候选。

灵活探索必须遵守以下约束：

- 探索得到的事实仍然必须可追溯；推断必须明确标为推断。
- 相邻能力只在当前功能域中记录接口和组合关系，不能为了省事把它的完整实现塞进当前目录。
- 新发现的独立能力应作为“候选功能域”报告；未完成采集前不能顺手标记为已验证。
- 探索默认只读源项目，不以验证想法为由擅自修改来源代码、数据或外部环境。
- 优先追踪会改变边界、不变量、红线、适用性或验证结论的信息，避免无目标地遍历整个仓库。
- 新证据推翻早期判断时，应回到对应步骤修正文档、示例和状态，而不是维护两个互相矛盾的说法。
- 当新增探索已经不会实质改变结论，或继续探索需要新的权限、环境或负责人决策时，应收敛并明确剩余未知项。

### 第 1 步：定义采集目标

先写清楚：

- 要采集的具体能力和用户价值；
- 功能边界、非目标和关键术语；
- 为什么认为它值得采集；
- 已知使用者、真实使用场景和运行时间；
- 候选源项目及负责人（如适用）。

如果一个目标包含多个可以独立使用的能力，先拆分功能域，再分别采集。

### 第 2 步：登记可追溯来源

对每个来源记录：

- 项目或仓库名称；
- 仓库地址或本地路径；
- 分支、tag 或精确 commit SHA；
- 采集日期；
- 关键文件路径；
- 相关文档、需求、事故记录、测试或监控证据；
- 适用的依赖版本和运行环境；
- 许可证或内部使用限制。

引用持续变化的分支名不够，必须尽量落到可复现的版本。不得采集密钥、令牌、个人数据、生产数据、内部地址或其他敏感信息。

### 第 3 步：还原问题和上下文

在看实现细节之前，先说明：

- 原始问题和约束是什么；
- 方案服务于哪些角色和业务流程；
- 数据规模、并发、安全、延迟、成本等条件；
- 与源项目其他模块的依赖；
- 哪些结论可能只适用于源项目。

缺少上下文的代码不能被认定为可迁移实践。

### 第 4 步：提炼设计，而不是复述代码

从来源中提炼并记录：

- 核心概念、实体、状态和生命周期；
- 模块边界、数据流和调用方向；
- 必须成立的不变量；
- 关键架构决策及其理由；
- 被否决或被替换的方案及原因；
- 扩展点、可配置项和有意保留的限制；
- 安全、权限、事务、并发、幂等、可观测性和故障恢复要求。

每个重要结论都应能追溯到代码、测试、文档、运行结果或事故经验。无法证实的内容标记为“假设”，不能包装成事实。

### 第 5 步：整理示例代码

示例代码是必需的，但它的作用是帮助 AI 理解设计和验证行为，不是提供整段复制源。

示例应当：

- 选择能说明关键边界的最小完整切片，优先端到端示例；
- 保留关键错误处理、权限校验、事务和并发语义，不能为了简短删掉安全条件；
- 去除源项目专属命名、无关业务逻辑和敏感信息；
- 标明语言、框架、依赖版本、运行方式和预期结果；
- 附带对应测试，或说明如何验证其行为；
- 标注来源文件和版本；发生改写时说明改写目的；
- 在代码前后解释“它证明什么”“不能从中推导什么”“迁移时哪些部分要重做”。

除非目标本身就是可复用库，否则不要复制整个模块或保留源项目目录结构。涉及第三方代码时必须保留许可和归属信息；许可不明确时只记录设计，不提交代码。

### 第 6 步：记录坑点、真实踩坑和红线

这三类内容必须分开记录：

- **坑点**：容易写错、误用或在特定条件下失败的地方，可以通过设计和测试规避。
- **真实踩坑**：源项目中实际发生过的事故、缺陷、返工或性能退化，是带来源的历史事实。
- **红线**：无论目标项目如何适配都不能突破的禁止事项或硬约束；触碰后可能造成安全、隐私、数据一致性、合规或重大可用性后果。

每条真实踩坑记录至少包含：

- 发生时间或对应版本；
- 适用环境与触发条件；
- 可观察症状和影响范围；
- 根因，而不只是表面修复；
- 当时采用的修复方式；
- 后续预防措施和回归测试；
- 事故、issue、commit、测试或维护者复核等证据位置。

每条红线至少包含：

- 明确禁止的行为；
- 红线成立的适用范围和依据；
- 触碰后可能产生的后果；
- 在设计、代码、评审或运行期如何强制执行；
- 用什么测试、扫描、监控或审计证明它没有被突破；
- 发现冲突时 AI 应停止的具体位置和需要谁来决策。

还应记录表面可行但已经失败的方案、容易被忽略的边界条件，以及哪些测试可以阻止问题再次发生。

如果没有已知事故，明确写“当前没有已知事故记录”，不能凭空编造反例。没有确认过红线时写“红线尚待确认”，不能把个人偏好提升为红线，也不能因为尚未记录就假定不存在红线。

### 第 7 步：建立验证证据

“已验证”必须说明在什么条件下、用什么证据验证。证据可以包括：

- 自动化测试及结果；
- 真实业务使用记录；
- 性能或容量测试；
- 安全评审和越权测试；
- 故障演练、恢复记录；
- 线上指标或事故修复后的回归证据；
- 与源项目维护者的复核结论。

验证结论必须有日期、环境和范围。某个维度没有验证时要明确列为剩余风险。

### 第 8 步：完成子功能域 `AGENTS.md`

按照第 4 节的固定结构，把采集结果整理为 AI 可以直接执行的说明，并给出目录内材料地图。示例、证据和结论之间应能相互定位。

### 第 9 步：执行独立复核

复核者或另一个 AI 至少检查：

- 是否能在不打开源项目的情况下理解功能；
- 事实、建议和假设是否清楚分离；
- 示例是否支持文档中的结论；
- 是否遗漏关键异常路径和安全边界；
- 真实踩坑是否可追溯，红线是否明确、可执行、可验证；
- 是否说明适配方法，而不是暗示照搬；
- 来源和版本是否可以追溯；
- 与其他功能域的组合关系是否清晰；
- 验证证据是否足以支持“已验证”状态。

有实质缺口时保持 `待验证`，修正后重新复核。

### 第 10 步：标记完成并维护时效

全部门禁通过后，才把状态改为 `已验证`，同时记录：

- 首次验证日期；
- 最近复核日期；
- 当前有效的来源版本；
- 已验证环境；
- 已知限制和剩余风险；
- 触发重新验证的条件。

来源依赖发生重大升级、安全模型变化、事故推翻原结论，或长期无人复核时，应改为 `已过期`，在重新完成相关步骤后才能恢复。

## 4. 子功能域 `AGENTS.md` 的固定结构

每个子功能域使用以下章节。允许增加内容，但不能省略必需章节；没有内容时明确写“暂无”并说明原因。

```markdown
# <功能名>

## 元数据
- 状态：
- 功能负责人：
- 首次验证日期：
- 最近复核日期：
- 已验证版本/环境：
- 来源索引：

## 1. 功能目标
解决的问题、用户价值和一句话能力定义。

## 2. 适用场景与非目标
什么时候使用、什么时候不使用，以及明确不负责什么。

## 3. 边界与依赖
输入、输出、上下游、信任边界和外部依赖。

## 4. 核心模型与不变量
概念、状态、生命周期，以及任何实现都必须保持的约束。

## 5. 已验证设计
架构、数据流、关键决策、取舍和证据定位。

## 6. 示例代码地图
逐项说明示例位置、用途、运行方法、来源和不能照搬的部分。

## 7. 适配新项目的方法
需要调查的目标项目条件、可替换点、建议步骤和迁移检查。

## 8. 坑点、真实踩坑与红线
分开记录常见坑点、已经发生的踩坑事实和绝对不能突破的红线。
真实踩坑包含时间/版本、触发条件、症状、影响、根因、修复、预防测试和证据。
红线包含禁止事项、适用范围、依据、违规后果、强制措施、验证方式和冲突升级方法。

## 9. 验证方法
验收标准、测试清单、性能/安全验证及已知证据。

## 10. 与其他功能域的组合
依赖、冲突、共享概念、责任归属和推荐组合顺序。

## 11. 来源与证据
精确版本、文件、文档、测试、运行证据和许可信息。

## 12. AI 使用指引
AI 阅读顺序、必须做的目标项目调查、禁止事项和输出要求。

## 13. 变更记录
只记录会影响理解、适用性或验证状态的实质变化。
```

## 5. 在新项目中使用 CBB 的固定流程

新项目可以组合多个功能域，但组合的是经过理解的设计能力，不是把多个示例目录合并到一起。

AI 必须：

1. 先调查目标项目的业务目标、技术栈、已有模型、约束和质量要求。
2. 选择可能相关的功能域，只采用状态为 `已验证` 且适用条件匹配的内容。
3. 阅读每个功能域的 `AGENTS.md`，列出要保留的不变量、可调整的实现选择和未满足的前置条件。
4. 为目标项目建立统一的领域边界、数据模型和架构，不能让各功能域分别发明用户、租户、权限、事件等共享概念。
5. 显式处理功能域之间的依赖和冲突；局部规则冲突时不得自行静默选择。
6. 汇总所有相关功能域的红线并逐项检查。目标需求或功能域之间触碰红线时，停止相关设计或实现，报告冲突并等待有权决策的人明确处理。
7. 产出适合目标项目的新设计和实现。示例代码只能作为证据与参考，不能默认原样复制。
8. 把采用了哪些 CBB 结论、做了哪些适配、为什么偏离参考方案记录在目标项目中。
9. 使用功能域提供的验收标准重新测试，并把历史踩坑转化为回归用例。源项目验证通过不代表新项目自动通过。

当功能域状态不是 `已验证`、目标条件明显不匹配、组合规则缺失或关键冲突无法解决时，AI 应停止把它当作最佳实践，明确报告缺口并先补充设计或验证。

## 6. 全局质量原则

- **证据优先**：区分事实、建议和假设；重要结论必须可追溯。
- **示例与解释并重**：既保留代码，也解释设计意图、适用边界和验证方式。
- **坑点和红线是一等资料**：成功方案、真实失败经验与不可突破的约束具有同等重要性。
- **允许灵活探索**：沿真实证据追踪必要的相邻边界，固定完成标准而不固定调查路线。
- **学习而非复制**：迁移不变量和经过验证的决策，根据目标上下文重做具体实现。
- **最小而完整**：只保留说明能力所需的材料，但不能删掉影响正确性的路径。
- **安全默认开启**：任何示例不得以简化为由省略必要的鉴权、数据隔离、输入校验或敏感信息保护。
- **组合优先考虑一致性**：共享概念由新项目统一设计，功能域不能形成互相矛盾的孤岛。
- **诚实描述成熟度**：没有证据就保持待验证；过期内容及时降级。
- **持续可维护**：来源变化、依赖升级或新事故出现时，及时更新结论和验证状态。

## 7. AI 在本仓库工作时的行为要求

- 修改任何功能域前，先读取根目录和该目录的 `AGENTS.md`。
- 新建功能域时先创建本地 `AGENTS.md` 并标记真实状态，再添加其他材料。
- 采集时主动检查调用方、测试、事故和边界模块；发现重要相邻能力时记录候选，不受最初文件清单限制。
- 不根据空目录、文件名或常识伪造“经过验证”的结论。
- 不删除来源、失败经验、红线或限制来让材料显得更整洁。
- 发现任务要求触碰红线时必须停止相关操作，指出具体冲突、依据和潜在后果；未经明确决策不得自行降低红线等级或绕过控制。
- 不把源项目的密钥、生产数据、内部地址或无关业务代码带入 CBB。
- 发现多个功能域边界重叠时，先记录冲突并澄清责任，不重复沉淀两套事实。
- 对内容作出实质修改时，同步检查状态、最近复核日期、来源索引、示例和验证证据。
- 如果任务只要求采集和整理，不得擅自修改源项目。
